/** Featured project selector retained as a small compatibility surface for
 * template components. Identity and order live only in projectCatalog. */
export { featuredProjects as projects } from '@/data/projectCatalog';
